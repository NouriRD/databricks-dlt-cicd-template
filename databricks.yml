bundle:
  name: databricks-dlt-cicd-template

targets:
  dev:
    workspace:
      root_path: /Workspace/Users/your_email@xxx.com/databricks-dlt-cicd-template
    mode: development
    default: true
  
  prod:
    mode: production

resources:
  pipelines:
    dlt_pipeline:
      name: dlt_pipeline
      libraries:
        - notebook:
            path: ./dlt_pipeline
      target: main.default
      continuous: false